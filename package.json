{
  "name": "lexical-research",
  "version": "0.0.0",
  "description": "This repository has the purpose to analyze the source code of Lexical, Meta's \r extensible text editor framework.",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "npm": ">=8.2.3"
  },
  "engineStrict": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "typescript": "^5.9.3"
  }
}
